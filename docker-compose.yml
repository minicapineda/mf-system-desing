services:
  mf-system-desing:
    container_name: mf-system-desing
    restart: unless-stopped
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 4173:4173
    volumes:
      - ./src:/app
      - ../packages/mf-types:/packages/mf-types
      - /app//node_modules